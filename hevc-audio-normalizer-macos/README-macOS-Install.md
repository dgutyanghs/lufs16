# macOS系统安装说明

## 快速开�?
2. 在终端中运行: npm install
3. 开发模式运�? npm run dev
4. 构建应用: npm run build-mac

## 依赖安装
```bash
# 安装Node.js依赖
npm install

# 如果需要创建图标，安装Python依赖
pip3 install Pillow
```

## 构建说明
- 使用 `npm run build-mac` 构建macOS版本
- 构建产物�?`dist/` 目录�?

## 注意事项
- 确保Xcode Command Line Tools已安�?

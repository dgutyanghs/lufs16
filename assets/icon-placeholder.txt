# 应用图标说明

## 图标要求
- 格式: PNG
- 尺寸: 512x512 像素
- 文件名: icon.png
- 位置: assets/icon.png

## 创建图标
1. 运行 `./create-icon.sh` 创建基本图标
2. 或者使用专业设计软件创建自定义图标
3. 确保图标文件命名为 `icon.png` 并放在 `assets/` 目录下

## 图标设计建议
- 使用简洁的设计
- 包含视频/音频相关的元素
- 使用高对比度的颜色
- 确保在小尺寸下仍然清晰可见

## 临时解决方案
如果没有图标文件，构建过程会使用默认图标，应用仍然可以正常工作。